$regex = '^(\d+) ((?!\1 ).)\d*?\s?(?!\2).$';
$regex = '^(\d+) ((?!\1 )\d*)\s?(?!\2).$';
$regex = '^(\d+) ((?!\1 )\d*)\s?((?!\2 )\d*)\s?$';
$regex = '^(\d+ )((?!\1)\d*\s?)((?!\2)\d*\s?)$';
# $regex = '^(\d+ )((?!(\1|\2))\d*\s?)*$';
$regex = '^(\d+)( (?!(\1 |\2))\d*)*$';
$regex = '^((\d+ |\s*(?!(\1| \1))\d*))*$';
# $regex = '^(\d+)( |((?!(\1\2|\2))\d*\s?))*$';
# $regex = '^(\d+ )(((?!\1)\d*\s?).)(((?!\2)\d*\s?).)$';
$test = <STDIN> ;
if($test =~ /$regex/){
    print "true";
} else {
    print "false";
}
