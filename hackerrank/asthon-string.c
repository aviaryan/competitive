#include "stdio.h"
#include "stdlib.h"
#include "math.h"
#include <string.h>
#include <stdbool.h>


int main(){
	long long t;
	scanf("%lld", &t);
	
	char str[100001];
	long long pos, slen;

	for (;t>0;t--){
		scanf("%s", str);
		scanf("%lld", &pos);
		int ep[100000] = {0};
		slen = strlen(str);

	}
}