#include "bits/stdc++.h"
#define MODULO 1000000007
#define endl '\n'
#define m(a) ((a)%MODULO)
using namespace std;

typedef unsigned long long ull;
typedef long long ll;

#define LT 100000

int main(){
	int t, i;
	ios_base::sync_with_stdio(0);
	char a[LT+1], b[LT+1], c[LT+1];

	cin >> a >> b >> c;
	int sa[26] = {0}, sb[26] = {0}, sc[26] = {0};

	// boring

	return 0;
}