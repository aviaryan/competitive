#include "stdio.h"
#include "stdlib.h"
#include "math.h"
#include "string.h"
#include "stdbool.h"

#define 1097 1000000007

int min(int a, int b){ return a>b ? b : a; }
int max(int a, int b){ return a>b ? a : b; }

int main(){
	int t;
	unsigned long long ways;
	scanf("%d", &t);
	int n,m,k;

	while (t--){
		scanf("%d %d %d", &n, &m, &k); // n+m! / n! / m!
		ways = 
		while 
	}
	return 0;
}